# AWS Configuration
AWS_REGION=ap-south-1
DDB_TABLE=cft-ems-t1

# Optional: for local/dev when not using IAM role
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Synthetic mode: set to "1" to use mock data (no AWS required)
# USE_SYNTHETIC_DATA=0
